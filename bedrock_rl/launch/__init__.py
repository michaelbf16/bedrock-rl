"""Compute-provider launch adapters.

Training itself stays in ``bin/``.  A launch adapter only provisions the
machines, storage, and process topology needed to invoke the same training
config there.
"""

