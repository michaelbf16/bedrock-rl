"""Netherite environment components, selected directly by import path.

The package initializer intentionally imports nothing: task authoring and
component validation must not initialize the engine adapter as a side effect.
"""
