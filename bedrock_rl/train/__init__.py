"""Training implementations and their lightweight input contracts.

Individual trainer modules import their optional frameworks. Importing the
package itself stays lightweight so schema and dataset helpers remain usable
in the base environment.
"""
